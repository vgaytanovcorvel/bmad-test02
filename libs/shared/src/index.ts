/**
 * Shared Library Public API
 * 
 * This module exports the complete public API of the shared library.
 * All shared types, utilities, and functionality are available through this barrel.
 * 
 * @example
 * ```typescript
 * import { Player, GameState, Move, GameConfig } from '@libs/shared';
 * ```
 * 
 * @since 2.1.0
 */

export * from './lib/shared';
export * from './lib/types';
export * from './lib/utils';
